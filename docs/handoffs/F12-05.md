# F12-05 Devir — Özet, sonuç ve müşteri yönetimi

## Başlangıç ve boyut

- Current base: `main` / `13a488696cc7844845d8219e6eee10ec4c6b87de`; `5423090..13a4886` yalnız `TASKS.md` factual state-sync değişikliğidir, runtime/test ağacı değişmemiştir.
- Initial claim base: `6c721472667ded7e492678969f42f542b469b9a5`
- Branch: `f12-05-public-group-booking`
- Boyut: L
- Doğrulama bütçesi: STRICT; public create idempotency/kurtarma davranışı ve gerçek tarayıcı yolculuğu değişiyor.
- Yetki sınırı: yeni migration, RPC, RLS veya auth authority değişikliği yok. Kabul edilmiş F11-02 `group-book`/recovery ve F11-03 group manage sözleşmeleri aynen tüketilir.

## Kapsam

- F12-04'ün doğrulanmış ordered `PublicMultiServiceSelectionState` sonucunu müşteri iletişim formuna bağlamak;
- aynı durable public booking intent kaydıyla `POST /api/public/business/:slug/group-book` çağrısı yapmak;
- create ve `/api/public/booking/resolve` cevaplarında group payload, recovery süresi ve exact management URL'ini doğrulamak;
- sonuçta hizmet sırasını, tarih/saat planını ve F12-03 min/max tahminini kesin tahsilat gibi göstermeden sunmak;
- kayıt durumu ile iletişim/bildirim durumunu birbirine karıştırmamak;
- belirsiz ağ cevabında ikinci create üretmeden resolve yapmak, `closed_absent` sonrasında planı koruyup yeni uygunluk seçimine dönmek;
- 360/390 px, klavye/focus, hata ilişkileri ve sabit eylem/taşma kabulini gerçek Chrome ile kanıtlamak;
- public route ilk JavaScript bütçesini ≤120 kB gzip olarak ölçmek ve operator/private ekranlarını eager yüklememek.

## Yazılabilir yüzey

- `src/PublicSalonPage.tsx`
- `src/PublicMultiServiceSelection.tsx` (yalnız tüketim/yenileme köprüsü gerekirse)
- `src/PublicBookingPage.tsx` veya aynı public flow'u tek sahipli tutan küçük ek component/type dosyaları
- `src/main.tsx` ve public CSS (route chunking/erişilebilirlik için)
- F12-05'e özel source/HTTP/browser regression testleri ve browser fixture'ları
- `TASKS.md`, bu devir ve exact-head kabul kanıtları

## Korunacak sözleşmeler

- F12-04 selection state ordered line sırasını ve `staffId: null` anlamını korur.
- F11-02 create body alanları, V2 proof/idempotency anahtarı, recovery secret ve management token semantiği değiştirilmez.
- F11-03 `/m#token` yönetim rotası ve grup CAS/version davranışı değiştirilmez.
- Pending store'da her salon/device için tek belirsiz işlem engeli korunur; ayrı component'ler aynı kaydın bağımsız sahibi olmaz.
- Range fiyatlar `Tahmini …–…`; eşit min/max `Tahmini …`; kesin tahsilat iddiası yoktur.
- Kampanya/yorum/sahte sosyal kanıt eklenmez.

## Kabul kapıları

- Typecheck, production build ve tüm HTTP/source regression kontrolleri;
- group create success, deterministic conflict, lost-response resolve, reload recovery ve duplicate-create engeli;
- gerçek Chrome'da 360/390 genişlikte müşteri formundan yönetim bağlantısına tam yolculuk;
- keyboard-only focus, alan hata ilişkileri, non-color durum ipuçları, yatay taşma ve kontrol yüksekliği;
- build manifest üzerinden public entry ve eager dependency gzip hesabı ≤120 kB;
- exact-head CI ve bağımsız R2; backend authority değişmediği sürece yeni R1 gerekmez.

## Aday ve yerel kanıt

- PR: [#187](https://github.com/ziyabeey1-ai/randevu/pull/187)
- Exact semantic implementation ve test edilen checkout: `22d8b2b778b715dc34d39d5914ded77337d95e01`; current-main base `13a488696cc7844845d8219e6eee10ec4c6b87de`.
- Exact-head full-code CI: [CI #1606 / run `35455573986`, job `105930120994`, attempt `1`](https://github.com/ziyabeey1-ai/randevu/actions/runs/35455573986/job/105930120994) — `22d8b2b778b715dc34d39d5914ded77337d95e01` checkout'u üzerinde **SUCCESS**.
- Okunan task skill'i: `supabase:supabase`; yeni Supabase migration/RPC/RLS/auth-authority yüzeyi açılmadığı ve kabul edilmiş F11 backend sözleşmeleri yalnız tüketildiği doğrulandı.
- `npm run build`: typecheck ve production build başarılı.
- `npm run test:http`: izinli ortamda 64 dosya / 789 test başarılı.
- `npm run test:public-bundle`: public initial graph 86.08 kB gzip / 120.00 kB; operator/private page chunk'ları eager graph dışında.
- Tam `test:browser-smoke`, açık macOS Chrome yolu ile başarılı. F12-05 gerçek production `main.tsx` rotaları ve lazy `PublicSalonPage` / `ManageAppointmentPage` chunk'ları üzerinden çalışır.
- F12-05 gerçek Chrome: 360/390 px normal create; görünür klavye focus; populated legacy katalogda `services_v2` hatasından gerçek `/book` tamamlama; unpinned personel yeniden ataması; pinned personel değişimi, satır zaman/fiyat ve geçersiz IANA timezone negatif kontrolleri; pre-F12 `bookingKind` alanı olmayan single intent'in aktif grup seçimi yanında scalar recovery'si; otomatik/manuel/reload recovery'de persisted immutable özgün plan; kullanıcı planı değiştirdikten sonra özgün sonucu ikinci create olmadan çözme; scheduled/confirmed için aktif yeşil, completed için explicit nötr, no_show/cancelled/partial için amber dikkat tonu hem büyük marker hem durum panelinde doğrulama; cancelled/partial committed sonuçlarında “oluşturuldu” yerine non-color durum-özel başlık, kayıt metni ve satır durumları; persisted group intent için scalar recovery reddi; `closed_absent` uygunluk yenilemesi; ordered result ve `/m#token` → gerçek grup yönetim ekranı başarılı.
- Mevcut F12-02/F12-04, S07 booking recovery, F11-03 group management/consumer ve legacy tek-hizmet regresyonları başarılı.

## R0 onarım ve kapanış

- Planner kullanılamadığında `groupMode` kapanır ve mevcut legacy tek-hizmet create/recovery yüzeyi erişilebilir kalır; başarılı `services_v2` planner açıkça group mode'u etkinleştirir.
- Create/recovery sonucu her ordered satırın service, start/end ve price type/min/max alanlarını seçili planla doğrular. `staffId: null` için sunucunun geçerli yeniden ataması kabul edilir; pinned personel değişemez.
- Production route/lazy-import kabulü doğrudan `src/main.tsx` üzerinden gerçek Chrome ile ölçülür.
- Create ve recovery grup payload'ları, render öncesinde `Intl.DateTimeFormat` ile geçerli IANA timezone taşıdığını doğrular; geçersiz timezone gerçek Chrome negatif senaryosunda fail-closed kalır.
- Aynı oturumdaki manuel recovery seçili grup planını yeniden doğrular. Gerçek-route Chrome, unpinned yeniden atamayı kabul ederken pinned personel değişimini ve manuel recovery'deki satır mutasyonunu fail-closed kanıtlar.
- V2 pending intent yeni kayıtlarda `single|group` türünü kalıcı taşır; F12-05 öncesi mevcut V2 kayıtları yalnız single-service olabildiği için eksik alan geriye uyumlu olarak `single` kabul edilir. Resolve, persisted tür ile scalar/group payload birebir eşleşmeden sonucu göstermez. Gerçek-route Chrome, grup create sonrası scalar committed cevabını hem aynı sayfada hem reload sonrası fail-closed kanıtlar.
- Pending group intent, create anındaki ordered seçim + slot + staff/time/price planını deep-copy immutable snapshot olarak saklar; automatic/manual/reload resolve yalnız bu özgün plana karşı doğrulanır. Kullanıcının daha sonra planner'ı değiştirmesi bekleyen sonucu zehirleyemez; single/pre-F12 intent'ler güncel grup seçimiyle karşılaştırılmaz.
- Copilot/Codex yorumları `4053093639`, `4053108918`, `4053108929`, `4053108943`, `4053161247`, `4053161260`, `4053253991`, `4053592466`, `4053592483`, `4053615343`, `4053700402`, `4053767853` ve `4053827073` bu onarım adayında ele alınmıştır. PR/TASKS/devir aynı güncel 64 dosya / 789 test sayısını kullanır; önceki 731/783 kayıtları superseded'dır. Final semantic `22d8b2b778b715dc34d39d5914ded77337d95e01`, full-code CI #1606 ile başarılıdır; temiz bounded R0 [`5743593165`](https://github.com/ziyabeey1-ai/randevu/pull/187#issuecomment-5743593165), açık review thread sayısı `0`dır. Açık tek kabul kapısı: bağımsız R2.

## Geri alma

Frontend entegrasyonu, route chunking ve F12-05 testleri geri alınabilir. F11 backend/migration sözleşmeleri ile F12-04 seçim yüzeyi bağımsız kalır.
