# F10-06 — Gerçek hesaplarla ortak yönetim kabulü

## Durum

**VERIFICATION hazır; repair'in main'e girmesini bekliyor.** DISCOVERY'nin üç runtime blocker'ı ve R2'nin zorunlu iki cross-tab leg'i artık gerçek Chrome'da doğrulama modunda koşuyor. Runtime düzeltmesi ayrı repair PR #157'de (F10-06R, #153); bu branch hâlâ yalnız acceptance/handoff taşır ve PR #113 draft kalır.

- Sahip: Ajan E; OPUS takeover (kullanıcı talimatı, 2026-09-18)
- Validation class: `reversible`
- Validation budget: `FOCUSED`
- Exact başlangıç base: `main@6ddb489b7c0f0849effb3a66e8fa076d3d70856d`
- Branch: `f10-06-real-account-management-acceptance`
- DISCOVERY frozen head: `bb6e0f2f84cd743cd6da6b9c6f1cc4ffbf3fabbf` (Issue #65 `5733687303`)
- Blocker-producing acceptance head: `34ff9d9e7153ccf0bed98417bc1e720a6c4007ee`
- Repair: PR #157 / Issue #153
- Draft PR: #113
- Coordinator assignment: Issue #65 comment `5712759823`
- Shared-runner fence: Issue #65 comment `5712775076`
- R2 current-main coherence blocker: Issue #65 comment `5713050174`
- R2 harness follow-up: Issue #65 comment `5713127894`
- Canonical Ajan E blocker receipt: Issue #65 comment `5713136113`

## Acceptance yüzeyi

Runner `scripts/browser-f10-management-acceptance.mjs`, production `src/main.tsx` bundle'ını Vite ile derler ve iki gerçek headless Chrome sekmesinde çalıştırır. UI production `App`, `OnboardingPage` ve `TeamPage` bileşenleridir; backend yalnız deterministic fixture'dır.

Kapsanan koreografi:

1. Salon A owner oturumu ile `/setup` açılışı.
2. Aynı hesapta Salon B manager üyeliğine gerçek işletme switch akışı.
3. `/team` erişimi ve manager authority görünümü.
4. Browser back/forward sonrası seçili işletmenin ve route'un korunması.
5. İkinci gerçek Chrome sekmesinde aynı manager state.
6. Server-side manager → staff rol düşüşü ve açık sekmede stale privileged mutation denemesi.
7. Fresh reload ile staff authority görünümü.
8. Membership deactivation sonrası eski team içeriğinin görünmemesi.
9. Başarılı yönetim mutation'ı sonrası transient `/api/team` 503.
10. Authenticated root workspace reload sırasında transient `/api/session` 503.
11. Logout/session-expiry davranışı.
12. Kullanıcı metninde `tenant`, `RPC`, `faz` taraması ve teslim edilmemiş mali aksiyonların aktif görünmemesi.

## Runtime blocker 1 — stale rol UI

Kod: `STALE_ROLE_UI_AFTER_403`

Repro:

1. Salon B için `/team` manager snapshot ile açık tutulur.
2. Server authority manager'dan staff'a düşürülür.
3. Açık sekmede stale `Davet oluştur` formu gönderilir.
4. Backend doğru şekilde 403 `NOT_ALLOWED` döndürür.
5. UI hata notice'ını gösterir fakat manager mutation kontrollerini görünür bırakır.
6. Aynı sayfa reload edildiğinde fresh staff snapshot gelir ve davet kontrolü kaybolur.

Sonuç: backend fail-closed, fakat açık sekme authority projection'ı stale kalıyor. Repair adayı `src/TeamPage.tsx` içinde privileged mutation denial sonrası authority refresh/invalidation davranışıdır.

## Runtime blocker 2 — transient team read doğrulanmış görünümü siliyor

Kod: `TRANSIENT_TEAM_READ_ERASES_AUTHORITY_VIEW`

Repro:

1. Manager yetkisiyle ekip daveti POST'u başarıyla tamamlanır.
2. Mutation sonrası yapılan authoritative `/api/team` refresh tek seferlik 503 alır.
3. Mevcut TeamPage yükleme davranışı `team=null` yapar.
4. Son doğrulanmış ekip/authority görünümü `Ekip alanı açılamadı` ekranına dönüşür.

Beklenen davranış: transient provider/read arızası son doğrulanmış state'i erişim kaybı gibi göstermemeli. Son snapshot tutulmalı, stale/retryable error açıkça belirtilmeli ve mutation sonucu yanlış başarı/başarısızlık anlamına çevrilmemeli.

## Runtime blocker 3 — transient session 503 logout gibi görünüyor

Kod: `TRANSIENT_SESSION_503_LOOKS_LOGGED_OUT`

Repro:

1. Root workspace authenticated manager state ile açılır.
2. Reload sırasında `/api/session` yalnız bir kez 503 `SESSION_UNAVAILABLE` döndürür.
3. `App` `Oturum şu anda doğrulanamıyor` notice'ını gösterir.
4. Fakat `session` null kaldığı için aynı anda `Çalışma alanına girin` login formu render edilir.

Sonuç: geçici auth/provider kesintisi kullanıcıya oturum kapanmış gibi görünür. Repair adayı `src/App.tsx` içinde loading / authenticated / anonymous / transient-session-error durumlarının ayrılmasıdır.

## R2 current-main coherence blocker'ları

R2 read-only inceleme `5713050174`, harness'ten bağımsız iki daha geniş session/business coherence sınıfını source üzerinden doğruladı:

### Cross-tab tenant drift

Aktif business origin-wide HttpOnly `yzt_business` cookie'sinde tutuluyor. İki tab A snapshot'ını render ederken Tab 1 B'yi seçerse cookie B olur fakat Tab 2 in-memory A gösterebilir. Business-scoped mutation server'da yalnız current shared cookie'den tenant çözerse kullanıcı A ekranında işlem yaparken durable write B'ye düşebilir.

Final F10-06 acceptance zorunluluğu: iki sekme A gösterirken Tab 1 → B select, Tab 2 reload olmadan business-scoped mutation denemesi. Beklenen sonuç B'ye sessiz durable write olmamasıdır. Repair ya stale tab'i mutation öncesi revalidate/invalidate etmeli ya da expected-business binding mismatch'ini sıfır durable write ile reddetmelidir.

### Cross-tab logout/session invalidation

Logout/session cookie değişikliği origin-wide olsa da açık management tab in-memory authenticated surface'i tutabilir. Final acceptance, diğer tab logout olduktan sonra stale tab'i **reload etmeden** focus/pageshow/authority check ile correct anonymous/session state'e düşürmeli ve hassas tenant-scoped UI datasını temizlemelidir.

R2 follow-up `5713127894`, blocker-producing head `34ff9d9e...` için bu iki leg'in mevcut harness'te henüz exact mutation/focus koreografisiyle kanıtlanmadığını teyit etti. Bu nedenle mevcut head acceptance candidate değildir.

## CI kanıtı

### CI #1125

- Run: `35212019179`
- Head: `493cb501f5faec3e8b356f2eb6d1cfb7941816aa`
- Sonuç: failure.
- Üç runtime blocker toplandı ancak runner tailindeki bağımsız path beklenti kusuru top-level failure metnini gölgeledi.

### CI #1126

- Run: `35212330699`
- Job: `105172543151`
- Head: `34ff9d9e7153ccf0bed98417bc1e720a6c4007ee`
- Sonuç: failure, yalnız F10-06 acceptance testinde.
- Top-level failure üç blocker kodunu açıkça raporluyor.
- Aynı run'da strict typecheck, production build, mevcut Chrome browser smoke, Worker dry-run ve diğer `628/629` Node/HTTP test yeşil.

## Doğrulama modu (VERIFICATION) ve cross-tab leg'leri

DISCOVERY harness'i kusurlu davranışı yakalamak için yazılmıştı; ikinci blocker adımı `Ekip alanı açılamadı` erase ekranının **görünmesini bekliyordu**. Doğru bir implementasyonda o ekran hiç çıkmadığı için adım zaman aşımıyla düşüyordu. Harness artık doğrulama modunda:

- **Blocker 2:** her iki implementasyonun da gösterdiği retryable okuma notice'ı (`Ekip bilgileri şu anda yenilenemiyor`) beklenir; erase ekranı görünür veya doğrulanmış ekip (`Salon B Çalışanı`) kaybolursa blocker kaydedilir.
- **Tail:** logout sonrası `/setup` kontrolü, `/team`'de duran `pageA` reload edilerek değil `/setup`'a gidilerek yapılır (bilinen harness-only kusur kapandı).
- **Teardown:** Chrome SIGTERM sonrası profilini yazmayı sürdürebildiği için `rmSync` tek denemede `ENOTEMPTY` atıp `finally` içinden gerçek sonucu gizliyordu; yeniden denemeli temizlik eklendi.
- **Fixture dürüstlüğü:** fixture, gerçek Worker'da olmayan hiçbir davranışı taklit etmez; yalnız her davet POST'unun hangi işletmeye yazıldığını ve durumunu kaydeder.

Yeni zorunlu leg'ler (R2 `5713050174`):

| Kod | Koreografi | Başarısızlık ölçütü |
| --- | --- | --- |
| `CROSS_TAB_TENANT_DRIFT_WRITE` (dönüşle) | İki sekme Salon B; Sekme 1 gerçek UI ile Salon A'yı seçer; kişi Sekme 2'ye reload etmeden döner (bringToFront + focus/visibilitychange/pageshow) ve gördüğü davet formunu kullanır | Sekme B gösterirken yazının 201 ile A'ya düşmesi |
| `CROSS_TAB_TENANT_DRIFT_WRITE` (sinyalsiz) | Aynı kayma, ama hiçbir dönüş sinyali olmadan bayat sekme doğrudan yazar | Sekme A gösterirken yazının 201 ile B'ye düşmesi |
| `CROSS_TAB_LOGOUT_STALE_SURFACE` | Sekme 1 çıkış yapar; açık ekip sekmesine reload etmeden dönülür | 3 sn içinde yönetim yüzeyinin (`Davet oluştur`, üye adları) kapanmaması |

Receipt artık `crossTab` alanında her leg'de sekmenin ne gösterdiğini, yazıların nereye düştüğünü ve logout yüzeyinin kapanıp kapanmadığını taşır; böylece "form yoktu, yazı olmadı" gibi boş bir geçiş ayırt edilebilir.

### Yerel gerçek-Chrome kanıtı (macOS, Chrome 2026-09)

| Ağaç | Sonuç |
| --- | --- |
| `main` (repair yok) | 5 blocker: üç DISCOVERY kodu + `CROSS_TAB_TENANT_DRIFT_WRITE` + `CROSS_TAB_LOGOUT_STALE_SURFACE`; tail artık zaman aşımına düşmüyor |
| `main` + #157 ilk üç düzeltme | Üç DISCOVERY blocker'ı kapanır; iki cross-tab leg düşer (dönüşlü ve sinyalsiz yazılar 201 ile görünmeyen işletmeye) |
| `main` + #157 `b579c26` | **3/3 koşu geçti.** Dönüşte sekme Salon A'ya yenilenip ancak sonra yazar; sinyalsiz yazı hiç gönderilmez (0 yazı) ve sayfa güncel bağlama yenilenir; logout yüzeyi kapanır |

**CI ortamında birleşik kanıt (Depot, Linux, 4 CPU):** aynı `ci.yml` main `8c9cb7b` + #157 `b579c26` src + bu branch'in acceptance dosyaları üzerinde koşturuldu (run `3kx6c4r10b`): **700/700**, F10-06 real-browser ✔, 10 zorunlu stage, CI gate passed. Bir önceki Depot koşusu (`1vw9k4nfnm`) harness'in kendi yarışını gösterdi: yeni açılan target ilk `about:blank` yüklemesini bitirmeden gönderilen CDP komutu `Inspected target navigated or closed` ile düştü. Bu yalnız sayfa açma/gezinme komutları için sınırlı yeniden deneme ile kapandı; tıklama ve form gönderimi asla tekrar edilmez.

Bu branch'in exact-head CI'ı, repair main'e girene kadar kasıtlı olarak kırmızı kalır: acceptance kendi ağacında (main) koşar ve yukarıdaki beş kodu raporlar. Repair merge edildikten sonra branch main'e taşınıp exact-head CI yeşil beklenir.

### Ertelenen gözlem (kritik değil, blocker setine eklenmedi)

#157'nin transient-session durumu, **açık bir çıkıştan hemen sonra** `/api/session` geçici 503 dönerse giriş formu yerine "Oturum durumu şu anda doğrulanamıyor … kaldığı yerden devam eder" panelini gösterir. Çıkış sunucuda başarıyla doğrulandığı için mesaj yanıltıcıdır; yetkili yüzey açılmaz, güvenlik etkisi yoktur. DISCOVERY kuralı gereği yeni kritik olmayan gözlem ertelenmiştir.

## Değişiklik sınırı

F10-06 acceptance branch'inde runtime product patch yoktur. Acceptance paketi:

- `scripts/browser-f10-management-acceptance.mjs`
- `tests/f10-management-acceptance.test.mjs`
- bu handoff

Dokunulmayan shared/production alanlar:

- `src/App.tsx`
- `src/TeamPage.tsx`
- `src/main.tsx`
- ortak router/session
- Worker/API
- Supabase/migrations
- `scripts/browser-smoke.sh`
- ortak CI workflow/runner listesi
- `scripts/ci-postgres-plan.json`
- lockfile
- shared global CSS

## Hosted / bağımsız review durumu

R2 current-main ve harness follow-up blocker'ları açık. Hosted staging auth/team acceptance henüz çağrılmadı. Hosted kanıt, repo-side blocker'ların üstünü örten bir geçiş kapısı olarak kullanılmayacak.

## Devam koşulu

1. ~~Coordinator session/business coherence + TeamPage/App error-state repair owner/token atar.~~ — #153 / #157.
2. Repair ayrı PR'da runtime davranışlarını düzeltir ve main'e girer. — #157 `b579c26` yerelde gerçek Chrome'da doğrulandı; **bağımsız inceleme + koordinatör merge bekliyor.**
3. F10-06 branch latest main'e taşınır. — yapıldı; repair merge'ünden sonra tekrarlanır.
4. ~~Runner tail path beklentisi temizlenir.~~
5. ~~R2'nin zorunlu tuttuğu cross-tab tenant-drift durable-write ve logout-without-reload leg'leri dedicated harness'e eklenir.~~
6. Exact-head F10-06 CI tamamen yeşil olur. — repair main'e girdikten sonra.
7. R2 bağımsız browser/integration review sonucu ACCEPTABLE olur.
8. Coordinator hosted staging auth/team acceptance'ı tetikler.
9. Ancak tüm kanıt zinciri yeşilse F10-06 kapanış/state-sync yapılır.
