# F13-01 — Takvim güncelliği ve istek yarışı

## Claim

- Status: **Üstlenildi / implementation handoff ready**
- Coordinator: ChatGPT / Sol
- Base main: `09e766d1f76f41a6d9919855f3925e24446efa37`
- Branch: `f13-01-calendar-freshness-race`
- Validation budget: **FOCUSED** by default
- Dependency: F11-03 — completed on main
- F12 lane: explicitly independent; do not edit or retarget F12-04/#114/#176 from this branch.

## Frozen task contract

Implement the existing Phase 13 F13-01 contract without broadening it:

1. Calendar data uses the canonical group/line projection already accepted by F11-03.
2. Requests are abortable and generation/version guarded so a stale response cannot overwrite a newer date, staff or business selection.
3. Visible-tab freshness is bounded to at most 30 seconds; focus return triggers one immediate refresh; hidden tabs do not poll aggressively.
4. Failed refresh preserves the last verified view with an explicit stale/retry state instead of silently erasing authority-backed data.
5. Business/session/permission changes invalidate in-flight and cached calendar context.
6. The S07 carry-forward race for mutable `appointments.starts_at` keyset continuation is owned here and must be reproduced with a real concurrent writer before closure.
7. Measure the current calendar network waterfall before optimizing it. Do not weaken auth or tenant authority merely to reduce hops.

## Writable scope

Primary lane:
- calendar request/data-loading code and focused tests;
- calendar-specific API adapter/query code;
- F13-01 handoff/receipt;
- this task's own `TASKS.md` row.

Conditional only after measured evidence:
- a bounded Worker/RPC change specifically for calendar read efficiency.

Not granted without a new coordinator claim:
- shared migration chain;
- `scripts/ci-postgres-plan.json`;
- unrelated router/entry/global CSS;
- F12 public booking code;
- F14/F16 financial/notification surfaces;
- auth/session authority semantics.

If an authority or DB contract change becomes necessary, stop and request the appropriate shared-writer token. That delta requires R1. Browser/calendar integration closure requires R2.

## Acceptance evidence

At minimum:

- stale response cannot overwrite a newer date/personnel/business filter;
- external operator/public booking appears within the bounded freshness window;
- hidden-tab polling is suppressed and focus causes a single refresh;
- transient network failure keeps verified data visible and exposes retry/stale state;
- revoked authority/business switch cannot leave old tenant data actionable;
- real concurrent `starts_at` mutation proves the chosen continuation/snapshot behavior without skip/repeat ambiguity;
- waterfall measurement is recorded before and after any hop-reduction change;
- exact-head CI is green.

## Explicit non-goals

- F13-02 day/week/list presentation work;
- F13-03 create/close/detail flows;
- F12-04/F12-05;
- new auth system, new polling framework, new generic cache platform;
- speculative performance refactors unrelated to the measured F13-01 path.

## Next concrete action

Measure the live current-main calendar request waterfall and inventory the exact calendar loader/API surfaces, then implement the smallest stale-request + freshness slice on this same branch.
